# Python bytecode and test caches.
__pycache__/
*.py[cod]
.pytest_cache/

# Local makepkg output. The PKGBUILD and app source remain tracked.
pkg/
src/
*.pkg.tar.*

# Release artifacts from packaging/build-release.sh. The build script and
# packaging templates remain tracked; only the built .deb/.rpm/.tar.gz go here.
dist/
